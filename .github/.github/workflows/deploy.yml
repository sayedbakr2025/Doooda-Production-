name: Deploy

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Deploy to server
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: 138.197.186.175
          username: root
          key: ${{ secrets.SSH_KEY }}
          script: |
            /app/deploy.sh
